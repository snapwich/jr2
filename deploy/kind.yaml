# Local kind cluster for j2. Create with: just kind-up
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: j2
nodes:
  - role: control-plane
    # extraMounts: mount a host path here later for the shared `default/` checkout
    # so Sandboxes can branch worktrees from it (see open design question on RWX/hostPath).
