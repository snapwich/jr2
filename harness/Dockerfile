# Sandbox image: runs the flue Harness hosting j2 Agents.
FROM node:22-slim

WORKDIR /app

# TODO: copy the built flue server from `flue build --target node`:
#   COPY dist/server.mjs ./server.mjs
#   CMD ["node", "server.mjs"]

CMD ["node", "-e", "console.log('j2 harness placeholder — not yet implemented')"]
