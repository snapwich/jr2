{
  "default": {
    "paths": ["*.feature"],
    "import": ["steps/**/*.ts"],
    "tags": "not @kind"
  },
  "kind": {
    "paths": ["*.feature"],
    "import": ["steps/**/*.ts"],
    "tags": "@kind"
  }
}
