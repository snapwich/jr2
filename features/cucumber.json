{
  "default": {
    "paths": ["*.feature"],
    "import": ["steps/**/*.ts"],
    "tags": "not @kind and not @console"
  },
  "kind": {
    "//": "Serial by default, but no longer because of a shared name: ADR-0040 made the wrap's `-base` intermediate per-converge scratch, and `--parallel 4` runs the tier green in ~1m45 (ADR-0010 has the measurements) — use it locally. What keeps the default serial is an undiagnosed, degree-independent flake: ~1 in 3 parallel runs loses one scenario whose first turn never reaches the scripted model (`menus seen: []`, converge clean). Diagnose that, then raise the default.",
    "paths": ["*.feature"],
    "import": ["steps/**/*.ts"],
    "tags": "@kind"
  },
  "console": {
    "paths": ["*.feature"],
    "import": ["steps/**/*.ts"],
    "tags": "@console"
  }
}
