# Orchestrator image: runs an xstate Machine in its own pod.
FROM node:22-slim

WORKDIR /app

# TODO: copy built dist/ + node_modules (or a bundled output) and run the Machine:
#   COPY dist/ ./dist/
#   CMD ["node", "dist/index.js"]

CMD ["node", "-e", "console.log('j2 orchestrator placeholder — not yet implemented')"]
